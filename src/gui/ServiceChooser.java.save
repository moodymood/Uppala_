package gui;

import java.awt.Frame;

import javax.swing.JOptionPane;

public class ServiceChooser {

	Object[] possibilities; = {"Dropbox", "Google Drive"};
	String cloudService;


	public ServiceChooser(){
		possibilities = Object[] possibilities = {"Dropbox", "Google Drive"};
		String cloudService = (String)JOptionPane.showInputDialog(
		                    new Frame(),
		                    "Choose your cloud service",
		                    "Customized Dialog",
		                    JOptionPane.PLAIN_MESSAGE,
		                    null,
		                    possibilities,
		                    "Dropbox");

		//If a string was returned, say so.
		if ((cloudService != null) && (cloudService.length() > 0)) {
		    if(cloudService.equals("Dropbox"))
		    	cloudType = "D";
		    else
		    	cloudType = "G";
		}else{
			System.out.println("Cancel");
		}
		cloudService  = (String)JOptionPane.showInputDialog(
	                    new Frame(),
	                    "Choose your cloud service",
	                    "Customized Dialog",
	                    JOptionPane.PLAIN_MESSAGE,
	                    null,
	                    possibilities,
	                    "Dropbox");

	//If a string was returned, say so.
	if ((cloudService != null) && (cloudService.length() > 0)) {
	    if(cloudService.equals("Dropbox"))
	    	cloudType = "D";
	    else
	    	cloudType = "G";
	}else{
		System.out.println("Cancel");
	}

}
